//Strings: Representan un texto, una cadena de caracteres

const producto = "Monitor de 20 pulgadas"; // Se utilizan comillas

const producto2 = String('Monitor de 22 pulgadas'); // Forma explicita de definir un string

const producto3 = new String('Monitor de 28 pulgadas'); // Usando new para reservar memoria y va a ser tipo Object

console.log(producto);
console.log(producto2);
console.log(producto3);